/**
 * Feature flags. Voice is fully built (realtime voice advisor, spoken replies /
 * TTS with the voice picker) but hidden for now — flip to true to bring it all
 * back. The code paths and API routes stay intact either way.
 */
export const VOICE_FEATURES = false;
